<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>NovaWallet.Domain</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Entities generate their own IDs (Uuid7) rather than relying on the database. -->
    <PackageReference Include="UUIDNext" />
  </ItemGroup>

</Project>
